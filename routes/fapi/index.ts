import KoaRouter from "koa-router";

// import RConfig from "./config";

const router = new KoaRouter();
// 设置路由
// router.use("/config", RConfig.routes(), RConfig.allowedMethods());

// 导出路由
export default router;
